# 东软环保公众监督系统需求规格说明书

## 一、文档说明

### 1.1 编写目的

本文档用于明确“东软环保公众监督系统”的业务目标、用户角色、功能范围、数据要求、接口约束和验收标准，为前端开发、后端开发、数据库设计、测试验收和课程展示提供统一依据。

### 1.2 项目名称

东软环保公众监督系统。

### 1.3 项目背景

随着公众环保意识提升，空气质量问题需要更及时地发现、上报、处理和统计。本系统面向公众监督员、系统管理员、网格员和决策者四类用户，支持公众提交空气质量反馈、管理员指派网格员、网格员确认检测数据、决策者查看统计大屏，形成较完整的空气质量监督业务闭环。

### 1.4 适用范围

本文档适用于本实训项目的基础版系统开发，范围包括前端四端页面、后端基础接口、数据库表结构、mock 演示数据和后续真实接口联调。

## 二、总体描述

### 2.1 系统目标

系统目标是完成空气质量公众监督业务的线上化管理，主要实现以下能力：

- 公众监督员能够注册、登录并提交空气质量反馈。
- 系统管理员能够查看公众反馈并指派网格员处理。
- 网格员能够查看被指派任务并提交 AQI 检测确认数据。
- 系统管理员能够查看检测统计明细。
- 决策者能够通过数据大屏查看 AQI 分布、地区统计、PM2.5 超标情况和网格覆盖情况。

### 2.2 用户角色

| 角色 | 说明 | 主要操作 |
| --- | --- | --- |
| 公众监督员 | 社会公众用户，负责上报空气质量问题 | 注册、登录、提交反馈、查看历史反馈 |
| 系统管理员 | 平台管理人员，负责处理公众反馈 | 登录、查看反馈、筛选反馈、指派网格员、查看统计 |
| 网格员 | 环保网格工作人员，负责现场检测确认 | 登录、查看任务、查看详情、提交检测数据 |
| 决策者 | 管理决策人员，关注整体空气质量趋势 | 查看数据大屏和统计图表 |

### 2.3 业务流程

```txt
公众监督员提交空气质量反馈
-> 系统管理员查看反馈并指派网格员
-> 网格员查看指派任务并提交检测确认数据
-> 系统管理员查看统计记录
-> 决策者查看数据可视化大屏
```

### 2.4 技术环境

| 类型 | 技术 |
| --- | --- |
| 前端框架 | Vue3、Vite |
| UI 组件库 | Element Plus |
| 路由 | Vue Router |
| HTTP 请求 | Axios |
| 图表 | ECharts |
| 后端框架 | Spring Boot |
| 数据访问 | MyBatis |
| 数据库 | MySQL |
| 数据格式 | JSON |

## 三、功能需求

### 3.1 首页角色入口

系统首页应展示四类角色入口：

- 公众监督员端。
- 网格员端。
- 系统管理员端。
- 决策者端。

用户点击对应入口后，应跳转到对应端的登录页或功能首页。

### 3.2 公众监督员端

#### 3.2.1 注册

公众监督员应能够填写真实姓名、手机号和密码进行注册。

功能要求：

- 手机号不能为空。
- 密码不能为空。
- 真实姓名不能为空。
- 手机号应具备唯一性。
- 注册成功后可进入公众监督员端业务页面。

#### 3.2.2 登录

公众监督员应能够使用手机号和密码登录。

功能要求：

- 登录成功后保存当前用户信息。
- 登录失败时提示错误原因。
- 未登录用户访问反馈提交页、历史反馈页时，应跳转到登录页。

#### 3.2.3 提交空气质量反馈

公众监督员应能够提交所在网格的空气质量反馈。

表单字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| 省份 | 下拉框 | 从省份接口动态加载 |
| 网格区域 | 下拉框 | 根据省份动态加载 |
| 预估 AQI | 滑块 | 范围 0-500，显示当前数值 |
| 反馈内容 | 多行文本 | 描述异味、扬尘、烟雾等问题 |

功能要求：

- 省份和网格区域不得为空。
- AQI 范围应在 0-500。
- 反馈内容不得为空。
- 提交成功后生成一条反馈记录，默认状态为“未处理”。

#### 3.2.4 历史反馈

公众监督员应能够查看本人提交过的反馈记录。

展示字段：

- 网格地址。
- 预估 AQI。
- 反馈内容。
- 处理状态。
- 提交时间。

### 3.3 系统管理员端

#### 3.3.1 管理员登录

管理员应能够使用管理员编码和密码登录。

默认演示账号：

| 账号 | 密码 |
| --- | --- |
| admin | 123456 |

#### 3.3.2 公众反馈管理

管理员应能够查看公众监督员提交的全部反馈。

展示字段：

- 网格地址。
- 公众监督员。
- 预估 AQI。
- 反馈内容。
- 状态。
- 提交时间。
- 操作。

功能要求：

- 支持按反馈状态筛选。
- 未处理反馈应显示“指派”操作。
- 已指派或已确认反馈应显示处理结果或网格员信息。

反馈状态规则：

| 状态值 | 状态名称 | 说明 |
| --- | --- | --- |
| 0 | 未处理 | 公众已提交，管理员未指派 |
| 1 | 已指派 | 管理员已指派网格员 |
| 2 | 已确认 | 网格员已提交检测确认数据 |

#### 3.3.3 指派网格员

管理员应能够将未处理反馈指派给对应地区的可用网格员。

功能要求：

- 页面展示反馈详情。
- 根据反馈所属省份和网格区域查询可用网格员。
- 管理员选择网格员后提交指派。
- 指派成功后反馈状态变为“已指派”。

#### 3.3.4 统计数据管理

管理员应能够查看网格员提交的 AQI 检测确认记录。

展示内容：

- 确认记录数量。
- 平均 AQI。
- PM2.5 超标次数。
- 覆盖网格数量。
- 地区检测统计。
- AQI 等级分布。
- 检测明细表格。

检测明细字段：

| 字段 | 说明 |
| --- | --- |
| 省份 | 反馈所属省份 |
| 网格区域 | 反馈所属区域 |
| 网格员 | 提交检测数据的网格员 |
| SO2 | SO2 浓度 |
| CO | CO 浓度 |
| PM2.5 | PM2.5 浓度 |
| 实测 AQI | 网格员实测 AQI |
| 确认时间 | 检测确认提交时间 |

### 3.4 网格员端

#### 3.4.1 网格员登录

网格员应能够使用网格员编码和密码登录。

默认演示账号：

| 账号 | 密码 |
| --- | --- |
| GM001-GM009 | 123456 |

#### 3.4.2 任务列表

网格员登录后，应只能查看指派给自己的任务。

展示字段：

- 网格地址。
- 公众监督员。
- 预估 AQI。
- 反馈内容。
- 状态。
- 指派时间。
- 操作。

#### 3.4.3 任务详情

网格员应能够查看任务完整信息。

展示内容：

- 反馈区域。
- 反馈描述。
- 公众监督员。
- 预估 AQI。
- 当前指派网格员。
- 提交时间。

#### 3.4.4 AQI 检测确认

网格员应能够提交现场检测数据。

表单字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| SO2 | 数字 | SO2 浓度 |
| CO | 数字 | CO 浓度 |
| PM2.5 | 数字 | PM2.5 浓度 |
| AQI | 数字 | 实测 AQI，范围 0-500 |

功能要求：

- 检测数据不得为空。
- 提交成功后生成统计记录。
- 提交成功后对应反馈状态变为“已确认”。

### 3.5 决策者端

#### 3.5.1 数据可视化大屏

决策者端应提供空气质量统计可视化页面。

展示内容：

- 确认数据总数。
- 平均 AQI。
- PM2.5 超标次数。
- 网格覆盖率。
- AQI 等级分布饼图。
- 地区检测数量柱状图。
- PM2.5 超标累计折线图。
- 网格覆盖率图表。

功能要求：

- 没有真实检测数据时，应展示默认 mock 数据，避免页面空白。
- 浏览器窗口变化时图表应自适应。
- 移动端宽度下应纵向排列，不产生明显横向溢出。

## 四、数据需求

### 4.1 核心数据实体

| 实体 | 说明 |
| --- | --- |
| Supervisor | 公众监督员 |
| Admins | 系统管理员 |
| GridProvince | 省份 |
| GridCity | 网格城市或区域 |
| GridMember | 网格员 |
| AqiFeedback | 空气质量反馈 |
| Statistics | AQI 检测确认统计 |

### 4.2 主要数据表

系统数据库应包含以下表：

- `supervisor`：公众监督员表。
- `admins`：系统管理员表。
- `grid_province`：省份表。
- `grid_city`：城市/区域表。
- `grid_member`：网格员表。
- `aqi_feedback`：AQI 反馈表。
- `statistics`：AQI 检测确认表。

### 4.3 AQI 等级规则

| AQI 范围 | 等级 |
| --- | --- |
| 0-50 | 优 |
| 51-100 | 良 |
| 101-150 | 轻度污染 |
| 151-200 | 中度污染 |
| 201-300 | 重度污染 |
| 301-500 | 严重污染 |

### 4.4 PM2.5 超标规则

基础版系统中，PM2.5 大于 75 时计为一次超标。

## 五、接口需求

### 5.1 统一约定

接口统一前缀：

```txt
/api
```

统一返回格式：

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 5.2 主要接口模块

| 模块 | 主要接口 |
| --- | --- |
| 公众监督员 | 注册、登录、根据 ID 查询 |
| 省市网格地址 | 查询省份、根据省份查询城市 |
| 空气质量反馈 | 保存反馈、查询历史反馈、查询全部反馈、指派网格员、查询网格员任务 |
| 管理员 | 管理员登录 |
| 网格员 | 网格员登录、根据省市查询可用网格员 |
| 统计 | 保存检测数据、查询统计列表、查询地区统计、查询 AQI 分布 |

### 5.3 前后端联调要求

- 前端默认使用 mock 数据，保证页面可独立演示。
- 设置 `VITE_USE_REAL_API=true` 后，前端通过 Axios 请求 Spring Boot 后端。
- 开发环境中 `/api` 代理到 `http://localhost:8080`。
- 后端接口返回字段与前端页面字段不一致时，由前端 `src/api` 层做字段适配。

## 六、非功能需求

### 6.1 易用性

- 页面布局应清晰，角色入口明确。
- 表单字段应有占位提示。
- 操作成功或失败时应有消息提示。
- 移动端 375px 宽度下核心页面应可正常使用。

### 6.2 可靠性

- 未登录用户访问受保护页面时应跳转到对应登录页。
- 表单提交前应做必要校验。
- 后端未启动时，mock 模式仍应支持完整演示流程。

### 6.3 可维护性

- 前端页面按角色目录划分。
- 接口按业务资源拆分，不直接在页面中写 Axios 请求。
- mock 数据集中放在 `src/mock`。
- 后端代码集中放在 `backend`。

### 6.4 兼容性

- 支持现代浏览器访问。
- 前端使用 hash 路由，避免后端未配置 history fallback 时刷新 404。
- 页面应适配桌面端和基础移动端宽度。

### 6.5 性能

- 页面加载应满足课程演示要求。
- 图表组件应支持窗口 resize。
- 列表数据量较小时可前端直接渲染，后续数据量增大时可扩展分页接口。

## 七、运行与部署需求

### 7.1 前端运行

```bash
npm install
npm run dev
```

前端默认访问地址：

```txt
http://127.0.0.1:5173/
```

### 7.2 后端运行

后端位于：

```txt
backend/
```

数据库初始化脚本：

```txt
backend/sql/init.sql
```

后端默认端口：

```txt
8080
```

### 7.3 真实接口模式

复制环境变量示例文件：

```bash
copy .env.example .env
```

将 `.env` 中配置改为：

```txt
VITE_USE_REAL_API=true
```

## 八、验收标准

### 8.1 功能验收

系统应完成以下演示流程：

1. 公众监督员登录或注册。
2. 公众监督员选择省份、网格区域并提交空气质量反馈。
3. 管理员登录后查看公众反馈列表。
4. 管理员将未处理反馈指派给对应网格员。
5. 网格员登录后查看被指派任务。
6. 网格员提交 SO2、CO、PM2.5、AQI 检测数据。
7. 管理员统计管理页显示检测记录。
8. 决策者数据大屏展示统计图表。

### 8.2 构建验收

前端应能成功执行：

```bash
npm run build
```

### 8.3 页面验收

- 首页四端入口可正常访问。
- 公众监督员端、管理员端、网格员端业务页具备登录保护。
- 决策者端可直接访问。
- 375px 移动端宽度下页面不应明显横向溢出。

### 8.4 数据验收

- 反馈状态能按“未处理 -> 已指派 -> 已确认”流转。
- 检测确认数据能进入统计管理页。
- 决策者大屏能展示统计结果。
- mock 模式和真实接口模式的页面字段展示应保持一致。

## 九、约束与假设

- 本系统为实训课程基础版，不包含复杂权限体系。
- 基础版不实现短信验证码、地图定位和消息推送。
- 基础版数据量较小，列表暂不强制分页。
- 后端真实接口联调依赖 MySQL、Spring Boot 服务正常启动。
- 前端保留 mock 模式，便于后端不可用时继续演示。

## 十、后续扩展建议

- 增加登录 token 和权限拦截。
- 增加反馈图片上传功能。
- 增加地图定位和网格地图展示。
- 增加管理员分页查询、关键字搜索和批量处理。
- 增加更多统计维度，如时间趋势、省份排名、污染物占比。
- 增加操作日志和异常告警功能。
