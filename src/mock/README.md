# mock

Mock 数据目录，用于后端接口未完成前支撑页面展示。

当前文件：

- `feedback.js`：公众反馈模拟数据。
- `gridRegion.js`：省份、网格区域模拟数据。
- `gridMember.js`：网格员模拟数据。
- `statistics.js`：统计图表模拟数据。

Mock 数据只用于前端开发阶段，后端接口完成后由 `src/api` 层统一替换。
