# 东软环保公众监督系统前端架构设计文档

## 一、架构目标

本项目采用四端基础版前端架构：

- 公众监督员端
- 系统管理员端
- 网格员端
- 决策者端

架构原则：

- 页面按角色划分。
- 接口按业务资源划分。
- mock 数据集中管理。
- 公共布局和图表组件复用。
- 后端联调时只替换 `src/api` 层。

## 二、技术栈

| 技术 | 用途 |
| --- | --- |
| Vue3 | 页面和响应式数据 |
| Vite | 构建与开发服务器 |
| Element Plus | UI 组件 |
| Vue Router | 四端路由 |
| Axios | 后端联调预留 |
| ECharts | 决策者端图表 |

## 三、目录结构

```txt
src/
  api/
  components/
  mock/
  router/
  utils/
  views/
    supervisor/
    admin/
    grid/
    decision/
```

## 四、核心模块

| 模块 | 说明 |
| --- | --- |
| `src/router/index.js` | 四端路由和登录守卫 |
| `src/utils/auth.js` | 公众监督员、管理员、网格员登录状态 |
| `src/utils/aqi.js` | AQI 等级和颜色计算 |
| `src/api/request.js` | Axios 后端联调预留 |
| `src/api/*.js` | 页面调用的业务接口函数 |
| `src/mock/*.js` | localStorage mock 数据 |

地址数据通过 `gridProvince.js`、`gridCity.js`、`gridRegion.js` 管理。公众反馈提交和管理员统计筛选统一从接口层读取省份、网格区域，避免页面内写死地址选项。

## 五、公共组件

| 组件 | 用途 |
| --- | --- |
| `RoleCard.vue` | 首页四端入口卡片 |
| `SupervisorShell.vue` | 公众监督员端页面外壳 |
| `AdminShell.vue` | 管理员端页面外壳 |
| `GridShell.vue` | 网格员端页面外壳 |
| `ChartPanel.vue` | ECharts 图表容器 |
| `PlaceholderPage.vue` | 备用占位页面 |

## 六、路由设计

| 路由 | 页面 |
| --- | --- |
| `/` | 首页角色入口 |
| `/supervisor/login` | 公众监督员登录 |
| `/supervisor/register` | 公众监督员注册 |
| `/supervisor/feedback` | 反馈提交 |
| `/supervisor/history` | 历史反馈 |
| `/admin/login` | 管理员登录 |
| `/admin/feedbacks` | 反馈管理 |
| `/admin/assign/:id` | 指派网格员 |
| `/admin/statistics` | 统计管理 |
| `/grid/login` | 网格员登录 |
| `/grid/tasks` | 任务列表 |
| `/grid/task/:id` | 任务详情 |
| `/grid/confirm/:id` | AQI 检测确认 |
| `/decision/dashboard` | 决策者数据大屏 |

路由守卫：

- 公众监督员业务页需要公众监督员登录。
- 管理员业务页需要管理员登录。
- 网格员业务页需要网格员登录。
- 决策者大屏不需要登录。

## 七、数据流

当前阶段：

```txt
页面 -> api 函数 -> mock 函数 -> localStorage
```

后端联调阶段：

```txt
页面 -> api 函数 -> request.js -> Spring Boot 接口
```

页面不直接访问 mock，也不直接写 Axios 请求。

接口切换：

- 默认 `VITE_USE_REAL_API=false`，使用 mock。
- 设置 `VITE_USE_REAL_API=true` 后，`src/api` 通过 Axios 请求 `/api`。
- Vite 开发代理已将 `/api` 转发到 `http://localhost:8080`。
- 后端目录已放入 `backend/`，数据库初始化脚本为 `backend/sql/init.sql`。

字段适配：

- 后端反馈列表返回 `supervisorRealName`，前端在 API 层统一映射为 `supervisorName`。
- 后端统计列表主要返回地区名称，管理员统计页已兼容按地区名称筛选。
- 后端 `saveStatistics` 会自动把反馈状态更新为已确认，前端检测确认后不再依赖额外真实确认接口。

## 八、业务闭环

```txt
公众监督员提交空气质量反馈
-> 管理员查看反馈并指派网格员
-> 网格员查看任务并提交检测数据
-> 管理员查看统计记录
-> 决策者查看统计大屏
```

当前前端已完成以上闭环的 mock 演示版本。
