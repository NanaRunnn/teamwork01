# 东软环保公众监督系统前后端接口文档

## 一、统一约定

默认接口前缀：

```txt
/api
```

统一返回格式：

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

当前前端仍使用 mock。后端完成后，前端会在 `src/api` 层替换为 Axios 请求。

## 二、公众监督员接口

### 注册

```txt
POST /api/supervisor/saveSupervisor
```

请求：

```json
{
  "phone": "13800138000",
  "password": "123456",
  "realName": "张三"
}
```

### 登录

```txt
POST /api/supervisor/login
```

请求：

```json
{
  "phone": "13800138000",
  "password": "123456"
}
```

返回：

```json
{
  "id": "S001",
  "phone": "13800138000",
  "realName": "张三",
  "role": "supervisor"
}
```

## 三、空气质量反馈接口

### 保存反馈

```txt
POST /api/aqiFeedback/saveAqiFeedback
```

请求：

```json
{
  "supervisorId": "S001",
  "supervisorName": "张三",
  "provinceId": "beijing",
  "provinceName": "北京市",
  "cityId": "chaoyang",
  "cityName": "朝阳区",
  "estimatedAqi": 86,
  "level": "良",
  "description": "附近空气有明显异味"
}
```

### 查询本人历史反馈

```txt
GET /api/aqiFeedback/listAqiFeedbackBySupervisorId?supervisorId=S001
```

### 查询全部反馈

```txt
GET /api/aqiFeedback/listAqiFeedbackAll
```

可选参数：

| 参数 | 说明 |
| --- | --- |
| status | 反馈状态 |

### 查询反馈详情

```txt
GET /api/aqiFeedback/getAqiFeedbackById?id=F001
```

### 指派网格员

```txt
POST /api/aqiFeedback/updateAqiFeedbackAssign
```

请求：

```json
{
  "feedbackId": "F001",
  "gridMemberId": "G001",
  "gridMemberName": "李四"
}
```

### 查询网格员任务

```txt
GET /api/aqiFeedback/listAqiFeedbackByGridMemberId?gridMemberId=G001
```

### 确认反馈状态

```txt
POST /api/aqiFeedback/confirmAqiFeedback
```

请求：

```json
{
  "feedbackId": "F001",
  "aqi": 95,
  "confirmTime": "2026-06-02 11:00:00"
}
```

反馈状态：

| status | 说明 |
| --- | --- |
| 0 | 未处理 |
| 1 | 已指派 |
| 2 | 已确认 |

## 四、管理员接口

### 登录

```txt
POST /api/admins/getAdminsByCode
```

请求：

```json
{
  "code": "admin",
  "password": "123456"
}
```

## 五、网格员接口

### 登录

```txt
POST /api/gridMember/login
```

请求：

```json
{
  "code": "GM001",
  "password": "123456"
}
```

### 根据地区查询可用网格员

```txt
GET /api/gridMember/listGridMemberByProvinceId?provinceId=beijing&cityId=chaoyang
```

返回：

```json
[
  {
    "id": "G001",
    "code": "GM001",
    "realName": "李四",
    "phone": "13900139000",
    "provinceId": "beijing",
    "cityId": "chaoyang",
    "cityName": "朝阳区",
    "workStatus": 1
  }
]
```

## 六、统计接口

### 保存 AQI 检测确认数据

```txt
POST /api/statistics/saveStatistics
```

请求：

```json
{
  "feedbackId": "F001",
  "gridMemberId": "G001",
  "gridMemberName": "李四",
  "provinceId": "beijing",
  "provinceName": "北京市",
  "cityId": "chaoyang",
  "cityName": "朝阳区",
  "so2": 20.5,
  "co": 0.8,
  "pm25": 68.2,
  "aqi": 95,
  "confirmTime": "2026-06-02 11:00:00"
}
```

### 查询检测确认列表

```txt
GET /api/statistics/listStatisticsAll
```

### 查询地区检测数量统计

```txt
GET /api/statistics/listProvinceItemTotalStatis
```

返回：

```json
[
  {
    "name": "朝阳区",
    "total": 3
  }
]
```

### 查询 AQI 等级分布

```txt
GET /api/statistics/listAqiDistributeStatis
```

返回：

```json
[
  {
    "level": "良",
    "total": 2
  }
]
```

### 查询 PM2.5 超标累计

```txt
GET /api/statistics/listPm25OverLimitStatis
```

### 查询网格覆盖率

```txt
GET /api/statistics/listGridCoverageStatis
```

返回：

```json
{
  "coveredCities": 3,
  "totalCities": 3,
  "rate": 100
}
```

## 七、AQI 等级规则

| AQI 范围 | 等级 |
| --- | --- |
| 0-50 | 优 |
| 51-100 | 良 |
| 101-150 | 轻度污染 |
| 151-200 | 中度污染 |
| 201-300 | 重度污染 |
| 301-500 | 严重污染 |
